services:
  mbomsign-coming-soon:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mbomsign-coming-soon
    environment:
      - NODE_ENV=production
      - PORT=4000
    volumes:
      - ./data:/app/data
    restart: unless-stopped
